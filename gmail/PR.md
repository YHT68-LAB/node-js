# Add React UI for Gmail Job Review

## Summary

- Added a React/Vite UI for reviewing Gmail job alert results in the browser.
- Added a local Node server with `/api/reviews` to fetch fresh Gmail data on page load or via the Refresh button.
- Preserved the existing static review generation flow by refactoring shared fetch/filter logic into `jobReviewService`.
- Added Gmail message metadata and per-email "Open email" links.
- Kept the fold/expand email view, with the first email expanded by default.
- Added editable source filters in the UI, with apply-on-refresh behavior and optional local default persistence.
- Added `/api/filters` for reading and writing local filter defaults.
- Added lightweight load timing in the UI and per-source timing in review API responses.
- Added Gmail query settings in `gmail-api.config.json` for search window, per-source result limit, source concurrency, and detail fetch concurrency.
- Tuned the benchmark Gmail fetch config to 14 days, 50 messages per source, `sourceConcurrency: 3`, and `messageDetailConcurrency: 5`.
- Split source layout into `server/src` for Node/Gmail/API code and `ui/src` for React code.
- Updated README with the new UI workflow.

## How to Run

```bash
npm run ui
```

Then open:

```text
http://localhost:5174
```

The UI uses the existing local Gmail token from `gmail-api.config.json`. No additional authorization flow is required when the token is already valid.

## Notes

- Page refresh and the in-page Refresh button both fetch fresh Gmail review data.
- Filter edits can be applied without changing source code, and **Save defaults** writes them to `job-filters.local.json`.
- React UI code changes require rebuilding with `npm run ui:build` while using the current production-style server flow.
- Existing CLI scripts such as `npm run reviewJobs` and `npm run reviewJobs:debug` are still supported.
- `frontend-dist/` is ignored because it is generated by the UI build.

## Changes Under `interview/`

- For this `dev` -> `main` PR, earlier commits also add interview practice implementations under `interview/`.
- Added dynamic programming and utility exercises:
  - `interview/src/dpCoins.ts`
  - `interview/src/fibo.ts`
  - `interview/src/get-primes.ts`
- Added LeetCode-style solutions:
  - `interview/src/leet-code/add-two-numbers.ts`
  - `interview/src/leet-code/longest-palindrome.ts`
  - `interview/src/leet-code/longest-substring.ts`
  - `interview/src/leet-code/median-of-two-sorted-arrays.ts`

## Testing

- [x] `npm run build`
- [x] `npm run ui:build`
- [x] Verified `http://localhost:5174` responds with `200`
- [x] Verified `/api/reviews` responds with `200` using the existing local Gmail token
